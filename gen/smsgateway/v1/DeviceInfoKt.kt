// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: smsgateway/v1/device.proto

@file:com.google.protobuf.Generated
// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package smsgateway.v1;

@kotlin.jvm.JvmName("-initializedeviceInfo")
public inline fun deviceInfo(block: smsgateway.v1.DeviceInfoKt.Dsl.() -> kotlin.Unit): smsgateway.v1.Device.DeviceInfo =
  smsgateway.v1.DeviceInfoKt.Dsl._create(smsgateway.v1.Device.DeviceInfo.newBuilder()).apply { block() }._build()
/**
 * ```
 * Descriptive information about a handset. Sent at enrollment and again on
 * every Register, so the server's view survives OS updates and SIM swaps.
 * ```
 *
 * Protobuf type `smsgateway.v1.DeviceInfo`
 */
public object DeviceInfoKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: smsgateway.v1.Device.DeviceInfo.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: smsgateway.v1.Device.DeviceInfo.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): smsgateway.v1.Device.DeviceInfo = _builder.build()

    /**
     * ```
     * Human-readable name for the operator ("desk phone", "SIM Orange 2").
     * ```
     *
     * `string label = 1 [json_name = "label"];`
     */
    public var label: kotlin.String
      @kotlin.jvm.JvmName("getLabel")
        get() = _builder.label
      @kotlin.jvm.JvmName("setLabel")
        set(value) {
        _builder.label = value
      }
    /**
     * ```
     * Human-readable name for the operator ("desk phone", "SIM Orange 2").
     * ```
     *
     * `string label = 1 [json_name = "label"];`
     */
    public fun clearLabel() {
      _builder.clearLabel()
    }

    /**
     * ```
     * The device's own MSISDN in E.164. This is the number recipients see, since
     * there is no custom sender ID. May be empty: Android cannot always read it.
     * ```
     *
     * `string phone_number = 2 [json_name = "phoneNumber"];`
     */
    public var phoneNumber: kotlin.String
      @kotlin.jvm.JvmName("getPhoneNumber")
        get() = _builder.phoneNumber
      @kotlin.jvm.JvmName("setPhoneNumber")
        set(value) {
        _builder.phoneNumber = value
      }
    /**
     * ```
     * The device's own MSISDN in E.164. This is the number recipients see, since
     * there is no custom sender ID. May be empty: Android cannot always read it.
     * ```
     *
     * `string phone_number = 2 [json_name = "phoneNumber"];`
     */
    public fun clearPhoneNumber() {
      _builder.clearPhoneNumber()
    }

    /**
     * `string manufacturer = 3 [json_name = "manufacturer"];`
     */
    public var manufacturer: kotlin.String
      @kotlin.jvm.JvmName("getManufacturer")
        get() = _builder.manufacturer
      @kotlin.jvm.JvmName("setManufacturer")
        set(value) {
        _builder.manufacturer = value
      }
    /**
     * `string manufacturer = 3 [json_name = "manufacturer"];`
     */
    public fun clearManufacturer() {
      _builder.clearManufacturer()
    }

    /**
     * `string model = 4 [json_name = "model"];`
     */
    public var model: kotlin.String
      @kotlin.jvm.JvmName("getModel")
        get() = _builder.model
      @kotlin.jvm.JvmName("setModel")
        set(value) {
        _builder.model = value
      }
    /**
     * `string model = 4 [json_name = "model"];`
     */
    public fun clearModel() {
      _builder.clearModel()
    }

    /**
     * ```
     * Android release, e.g. "14". Also a hint for platform-specific SMS quirks.
     * ```
     *
     * `string os_version = 5 [json_name = "osVersion"];`
     */
    public var osVersion: kotlin.String
      @kotlin.jvm.JvmName("getOsVersion")
        get() = _builder.osVersion
      @kotlin.jvm.JvmName("setOsVersion")
        set(value) {
        _builder.osVersion = value
      }
    /**
     * ```
     * Android release, e.g. "14". Also a hint for platform-specific SMS quirks.
     * ```
     *
     * `string os_version = 5 [json_name = "osVersion"];`
     */
    public fun clearOsVersion() {
      _builder.clearOsVersion()
    }

    /**
     * ```
     * Agent app version. Needed to interpret behaviour during a staged rollout.
     * ```
     *
     * `string agent_version = 6 [json_name = "agentVersion"];`
     */
    public var agentVersion: kotlin.String
      @kotlin.jvm.JvmName("getAgentVersion")
        get() = _builder.agentVersion
      @kotlin.jvm.JvmName("setAgentVersion")
        set(value) {
        _builder.agentVersion = value
      }
    /**
     * ```
     * Agent app version. Needed to interpret behaviour during a staged rollout.
     * ```
     *
     * `string agent_version = 6 [json_name = "agentVersion"];`
     */
    public fun clearAgentVersion() {
      _builder.clearAgentVersion()
    }

    /**
     * ```
     * Network operator from the SIM. Relevant because sending limits are per
     * carrier.
     * ```
     *
     * `string carrier = 7 [json_name = "carrier"];`
     */
    public var carrier: kotlin.String
      @kotlin.jvm.JvmName("getCarrier")
        get() = _builder.carrier
      @kotlin.jvm.JvmName("setCarrier")
        set(value) {
        _builder.carrier = value
      }
    /**
     * ```
     * Network operator from the SIM. Relevant because sending limits are per
     * carrier.
     * ```
     *
     * `string carrier = 7 [json_name = "carrier"];`
     */
    public fun clearCarrier() {
      _builder.clearCarrier()
    }

    /**
     * An uninstantiable, behaviorless type to represent the field in
     * generics.
     */
    @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
    public class SimsProxy private constructor() : com.google.protobuf.kotlin.DslProxy()
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     */
     public val sims: com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>
      @kotlin.jvm.JvmSynthetic
  get() = com.google.protobuf.kotlin.DslList(
        _builder.simsList
      )
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     * @param value The sims to add.
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("addSims")
    public fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.add(value: smsgateway.v1.Device.SimInfo) {
      _builder.addSims(value)
    }
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     * @param value The sims to add.
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("plusAssignSims")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.plusAssign(value: smsgateway.v1.Device.SimInfo) {
      add(value)
    }
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     * @param values The sims to add.
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("addAllSims")
    public fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.addAll(values: kotlin.collections.Iterable<smsgateway.v1.Device.SimInfo>) {
      _builder.addAllSims(values)
    }
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     * @param values The sims to add.
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("plusAssignAllSims")
    @Suppress("NOTHING_TO_INLINE")
    public inline operator fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.plusAssign(values: kotlin.collections.Iterable<smsgateway.v1.Device.SimInfo>) {
      addAll(values)
    }
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     * @param index The index to set the value at.
     * @param value The sims to set.
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("setSims")
    public operator fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.set(index: kotlin.Int, value: smsgateway.v1.Device.SimInfo) {
      _builder.setSims(index, value)
    }
    /**
     * ```
     * Every SIM the handset can send from.
     *
     * phone_number and carrier above describe the *default* SMS subscription
     * only, which is what a single-SIM phone reports and what a dual-SIM phone
     * would send from if nobody said otherwise. They stay because every existing
     * agent fills them and every existing row was written from them.
     *
     * A dual-SIM handset is really two sending nodes: sending limits are per SIM
     * and the number a recipient sees is per SIM. Listing them lets a caller
     * name one on SendSmsJob instead of being stuck with whatever the phone's
     * settings happen to say.
     * ```
     *
     * `repeated .smsgateway.v1.SimInfo sims = 8 [json_name = "sims"];`
     */
    @kotlin.jvm.JvmSynthetic
@kotlin.jvm.JvmName("clearSims")
    public fun com.google.protobuf.kotlin.DslList<smsgateway.v1.Device.SimInfo, SimsProxy>.clear() {
      _builder.clearSims()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun smsgateway.v1.Device.DeviceInfo.copy(block: `smsgateway.v1`.DeviceInfoKt.Dsl.() -> kotlin.Unit): smsgateway.v1.Device.DeviceInfo =
  `smsgateway.v1`.DeviceInfoKt.Dsl._create(this.toBuilder()).apply { block() }._build()

